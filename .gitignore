# Credentials — never commit
.env

# Compiled binaries
*.exe
*.zip
/tg

# Release bundles
dist/

# Runtime data the bridge writes (received files, etc.) — may be private
tg-uploads/

# Air hot-reload temp dir
tmp/

# OS
.DS_Store
Thumbs.db
